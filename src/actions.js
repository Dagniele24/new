export const setSearchQuery = (query) => ({
    type: 'SET_SEARCH_QUERY',
    payload: query,
  });
  
  export const setJobResults = (jobs) => ({
    type: 'SET_JOB_RESULTS',
    payload: jobs,
  });
  